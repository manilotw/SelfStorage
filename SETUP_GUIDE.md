# 🚀 Руководство по запуску SelfStorage

## Предварительные требования

```bash
python 3.8+
Django 3.2
Celery (для автоматических напоминаний)
```

## Установка и запуск

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Применение миграций
```bash
python manage.py migrate
```

### 3. Создание суперпользователя (администратора)
```bash
python manage.py createsuperuser
```

### 4. Запуск разработческого сервера
```bash
python manage.py runserver
```

Сервер будет доступен по адресу: `http://127.0.0.1:8000`

---

## 💼 Администраторская панель

Перейдите на `http://127.0.0.1:8000/admin/`

Там вы можете:
- ➕ Добавить **Склады** (Storage)
- ➕ Добавить **Боксы** (Box)
- ➕ Создать **Тарифы** (Tariff)
- 👀 Просмотреть **Заказы** (Order) с их статусами

---

## 📚 Создание тестовых данных

### Создать тариф (30 дней, 1000 ₽):
```bash
python manage.py shell
```

```python
from stock_app.models import Tariff
Tariff.objects.create(title='Месячная аренда', price=1000, days=30)
```

### Создать склад:
```python
from stock_app.models import Storage
Storage.objects.create(
    title='Склад №1',
    address='ул. Примерная, д. 123',
    image='path/to/image.jpg',
    slug='storage-1',
    status=True
)
```

### Создать боксы:
```python
from stock_app.models import Box, Storage
storage = Storage.objects.first()
Box.objects.create(
    title='101',
    storage=storage,
    price=1000,
    length=2.0,
    width=1.5,
    height=2.0,
    status=True
)
```

---

## 🔔 Автоматические напоминания

### Запуск Celery Worker (в отдельном терминале):
```bash
celery -A mailapp worker -l info
```

### Запуск Celery Beat (в отдельном терминале):
```bash
celery -A mailapp beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
```

### Или вручную отправить напоминания:
```bash
python manage.py send_rent_reminders
```

---

## 🧪 Тестирование системы

### Запустить тест аренды:
```bash
python test_rental.py
```

Это создаст тестового пользователя и пройдет через весь цикл аренды.

---

## 📧 Конфигурация email

Текущая реализация использует заглушку для email (вывод в консоль).

Для реального использования отредактируйте `settings.py`:

```python
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your-email@gmail.com'
EMAIL_HOST_PASSWORD = 'your-app-password'
DEFAULT_FROM_EMAIL = 'your-email@gmail.com'
```

---

## 🔐 Переменные окружения

Создайте файл `.env` в корневой директории:

```
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
YOOKASSA_SHOP_ID=your-shop-id
YOOKASSA_API_KEY=your-api-key
```

---

## 🐛 Решение проблем

### Ошибка "Box matching query does not exist":
- Убедитесь, что у вас есть хотя бы один бокс в БД
- Перейдите в админ-панель и создайте бокс

### Ошибка при платеже Yookassa:
- Проверьте YOOKASSA_SHOP_ID и YOOKASSA_API_KEY в .env
- Убедитесь, что тариф существует (дни = 30)

### Celery не запускается:
- Убедитесь, что Redis установлен (если используется как broker)
- Или используйте SQLAlchemy для SQLite

---

## 📁 Структура проекта

```
storage/
├── stock_app/               # Основное приложение
│   ├── models.py           # Модели (Box, Order, Tariff, Storage, User)
│   ├── views.py            # Представления (views)
│   ├── forms.py            # Формы
│   ├── admin.py            # Admin interface
│   ├── backends.py         # Authentication backend
│   ├── management/
│   │   └── commands/
│   │       └── send_rent_reminders.py  # Команда отправки напоминаний
│   ├── templates/          # HTML шаблоны
│   │   ├── boxes.html
│   │   ├── payment.html
│   │   ├── my-rent.html
│   │   ├── rental-history.html
│   │   └── include/
│   └── migrations/         # Миграции БД
│
├── mailapp/                # Приложение для email
│   ├── tasks.py           # Celery tasks
│   ├── celery.py          # Celery configuration
│   └── templates/
│
├── templates/             # Основные шаблоны
│   └── base.html
│
├── storage/              # Конфигурация проекта
│   ├── settings.py
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
│
├── manage.py
├── requirements.txt
└── db.sqlite3            # База данных
```

---

## 📞 Техническая поддержка

При возникновении проблем проверьте:
1. Логи в консоли Django
2. Админ-панель для просмотра данных
3. Базу данных на наличие необходимых записей
4. Переменные окружения в .env файле

---

**Система готова к использованию! 🎉**
